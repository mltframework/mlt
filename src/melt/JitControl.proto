syntax = "proto2";

message JitControl {
    optional ControlType type = 1;
    optional int32 play_rate = 2;
    optional int64 seek_position = 3;
}

enum ControlType {
    PAUSE = 0;
    PLAY = 1;
    PLAY_RATE = 2;
    QUIT = 3;
    SEEK = 4;
}
