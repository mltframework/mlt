#!/bin/sh

if [ "$help" != "1" ]
then

	bits=$(uname -m)
	case $bits in
	ppc|ppc64)
		disable_xine="1"
		;;
	*)
		disable_xine="0"
		;;
	esac

	if [ "$disable_xine" != "0" ]
	then
		echo "- MMX Capabalities not found: disabling xine deinterlacing module"
		touch ../disable-xine
	fi
	exit 0
fi

