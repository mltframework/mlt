#!/bin/sh

if [ "$help" != "1" ]
then

cat << EOF >> ../producers.dat
EOF

cat << EOF >> ../filters.dat
BurningTV	libmlteffectv$LIBSUF
EOF

cat << EOF >> ../transitions.dat
EOF

cat << EOF >> ../consumers.dat
EOF

fi
