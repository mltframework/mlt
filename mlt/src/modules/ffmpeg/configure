#!/bin/bash

if [ "$help" != "1" ]
then

cat << EOF >> ../producers.dat
ffmpeg			libmltffmpeg.so
EOF

cat << EOF >> ../filters.dat
ffmpeg_dub		libmltffmpeg.so
EOF

cat << EOF >> ../consumers.dat
ffmpeg			libmltffmpeg.so
EOF

fi

